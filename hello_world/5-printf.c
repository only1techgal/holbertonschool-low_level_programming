#include <stdio.h>

/**
 * main - prints "with proper grammer, but the outcome is a piece of art, followed by a new line
 *
 * Return : Always 0 (Success)
 */
int main(void)
{
 printf("with proper grammer, but the outcome is a piece of art,\n");
	Return (0);
}
 
