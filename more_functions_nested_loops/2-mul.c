#include "main.h"
#include <stdio.h>

/**
 * main - This multipies integers
 *
 * Return: Always 0.
 */
int mul(int a, int b)
{
	return (a * b);
}
	int function_pointer;
	function_pointer = mul;
	printf("The product of two numbers is:%d", (98, 1024));
	printf("The product of two numbers is:%d", (-402, 4096));
}

	

